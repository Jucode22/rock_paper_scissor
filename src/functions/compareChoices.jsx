import React from "react";

//return the winner
// update the score
// update the round
// alert the user that it was either a tie, win, or lose

//have a div that holds 2 spans
// the two spans must have the currentRound and the Score displayed

const CompareChoices = () => {};

export default CompareChoices;
